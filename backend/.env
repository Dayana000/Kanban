# Puerto en el que se ejecutará el servidor backend de Express.
# Si no se define, normalmente toma el 3000 por defecto, 
# pero aquí se configuró explícitamente en 3001.
PORT=3001

# URL del webhook de n8n al que el backend enviará eventos.
# http://localhost:5678 → es la dirección y puerto donde corre n8n localmente.
# /webhook/tekai-events → es el endpoint específico del flujo en n8n
# encargado de recibir y procesar los eventos del sistema (crear, actualizar, eliminar tareas).
N8N_WEBHOOK_URL=http://localhost:5678/webhook/tekai-events
