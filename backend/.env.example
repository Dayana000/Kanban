# Puerto en el que se ejecutará el servidor backend de Express.
# Ejemplo: si PORT=3001, la API estará disponible en http://localhost:3001
PORT=3001

# URL del webhook de n8n al que el backend enviará eventos (crear, actualizar, eliminar tareas).
# Aquí debes poner la URL completa de tu flujo en n8n.
# Ejemplo local: http://localhost:5678/webhook/tekai-events
# Ejemplo en producción: https://mi-servidor.com/webhook/tekai-events
N8N_WEBHOOK_URL=
